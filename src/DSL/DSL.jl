# ─── CQL Julia-native DSL ─────────────────────────────────────────────────────
#
# Provides @typeside, @schema, @instance, @mapping, @query macros
# and Unicode operators Δ, Σ, ∘ for CQL operations.

include("Helpers.jl")
include("Operators.jl")
include("Macros.jl")
